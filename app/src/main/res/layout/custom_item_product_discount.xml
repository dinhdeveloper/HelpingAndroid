<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="200dp"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="@dimen/dp5"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardPreventCornerOverlap="false"
    android:id="@+id/layout_item"
    app:cardBackgroundColor="@color/bg_"
    app:cardElevation="5dp"
    app:layout_constraintCircleRadius="@dimen/dp5"
    app:cardCornerRadius="@dimen/dp5">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <ImageView
                android:id="@+id/imvProduct"
                android:layout_gravity="center"
                android:scaleType="centerCrop"
                android:layout_width="match_parent"
                android:layout_height="135dp"/>
            <ImageView
                android:layout_alignRight="@+id/imvProduct"
                android:src="@drawable/ic_discount_dinh"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:id="@+id/tvDiscount"
                android:layout_margin="3dp"
                android:layout_alignRight="@+id/imvProduct"
                android:textSize="10sp"
                android:textStyle="normal"
                android:textColor="@color/white"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="80%"/>
        </RelativeLayout>
        <!--end-->
        <TextView
            android:id="@+id/tvName"
            android:fontFamily="@font/gotu_regular"
            android:paddingTop="@dimen/dp5"
            android:paddingHorizontal="@dimen/dp5"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Iphone 12 Pro max"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="16sp"
            android:layout_gravity="center"
            android:gravity="start|center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/tvPrice"
            android:fontFamily="@font/gotu_regular"
            android:paddingHorizontal="@dimen/dp5"
            android:paddingBottom="@dimen/dp5"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="120.000"
            android:textColor="@color/color_price"
            android:textStyle="normal"
            android:textSize="13sp"
            android:layout_gravity="center"
            android:gravity="start|center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </LinearLayout>

</androidx.cardview.widget.CardView>